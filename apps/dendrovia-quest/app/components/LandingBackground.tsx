'use client';

/**
 * LandingBackground — Client island wrapping ParticleBackground.
 * Shader-bg is provided by CSS in the browse layout.
 */
import { ParticleBackground } from './ParticleBackground';

export function LandingBackground() {
  return <ParticleBackground />;
}
